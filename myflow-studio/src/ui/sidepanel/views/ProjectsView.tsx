import { PlaceholderView } from "@ui/components";

export function ProjectsView() {
  return (
    <PlaceholderView
      milestone="M10 — Projects"
      title="Projects"
      description="Create, open, save, duplicate, export, and import project bundles — your prompts, settings, and history as one unit."
    />
  );
}
