import { PlaceholderView } from "@ui/components";

export function LogsView() {
  return (
    <PlaceholderView
      milestone="M11 — Monitoring & debug tooling"
      title="Logs"
      description="A live, filterable log of every automation step, download, and error arrives here."
    />
  );
}
