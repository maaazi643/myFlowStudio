import { PlaceholderView } from "@ui/components";

export function MonitorView() {
  return (
    <PlaceholderView
      milestone="M11 — Monitoring & debug tooling"
      title="Monitor"
      description="Performance and health monitors, plus the Selector Inspector for diagnosing Google Flow DOM drift, live here."
    />
  );
}
