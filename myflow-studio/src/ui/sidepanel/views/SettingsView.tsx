import { PlaceholderView } from "@ui/components";

export function SettingsView() {
  return (
    <PlaceholderView
      milestone="M4 — Generation Settings module"
      title="Settings"
      description="Speed, images per prompt, aspect ratio, model, quality, numbering, and the live filename preview live here."
    />
  );
}
