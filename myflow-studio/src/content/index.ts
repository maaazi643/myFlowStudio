// Bootstrap only. The automation state machine, selector registry, and
// observers arrive in M6 (automation engine core), gated by the SPIKE
// milestone's DOM reconnaissance of Google Flow.

console.info("[MyFlow Studio] content script injected on", window.location.href);
